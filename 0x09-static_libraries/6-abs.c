#include "main.h"

/**
*_abs - absolute value of an integer
*@n: is the value to be checked
*
*Return: the absolute value of an int
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
